import React, { FC } from 'react';
import Toolbar from '~component/Toolbar';

const Settings: FC = () => {
  return (
    <div>
      <Toolbar></Toolbar>
      <div>Settings</div>
    </div>
  );
};

export default Settings;
